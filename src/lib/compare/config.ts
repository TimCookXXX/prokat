// Параметры выдачи (ТЗ, раздел 10). Стартовые гипотезы — уточняются по lead_events.
// Все настраиваемые числа сравнения живут здесь, а не по месту использования.

/** Цена старше — «на перепроверке», вне рейтинга. */
export const STALE_AFTER_DAYS = 30;
/** Извилистость дорог: расстояние по прямой × коэффициент. */
export const ROUTE_FACTOR = 1.3;
/** Средняя скорость по городу, км/ч. */
export const CITY_SPEED_KMH = 25;
/** Стоимость минуты в пути, ₽ — для оценки «Оптимальный». */
export const MINUTE_COST_RUB = 10;
/** Поездок в оценке: забрать (туда и обратно) и вернуть (туда и обратно). */
export const TRIPS_PER_RENTAL = 4;
/** Разброс итогов меньше этой доли — по умолчанию вкладка «Ближе всего». */
export const LOW_SPREAD_SHARE = 0.15;
/** Недельный тариф дешевле 7 × суточной хотя бы на эту долю — подсказка про неделю. */
export const WEEK_HINT_MIN_SHARE = 0.1;
/** Задержка подсказок адресов от геокодера, мс. */
export const GEOCODER_DEBOUNCE_MS = 300;
